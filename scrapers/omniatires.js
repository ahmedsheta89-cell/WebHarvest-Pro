/**
 * WebHarvest Pro - OmniaTires Scraper
 *     // Price in AED
        const priceElement = 
[truncated]
elector('.short-description')?.textContent?.trim() || '';
        
        // Specification
[truncated]
 [];
    }
}

// Export
export default new OmniaTiresScraper();
