/**
 * WebHarvest Pro - HSN Scraper
 *     // Price
        const priceElement = $('.pdp-pricing__sale') || $('.pdp-pricing__list');
        const price = this.parse
[truncated]
etal')?.attr('content') || '';
        
        // Brand
        const brand = $('meta[property="product:brand"]')
[truncated]
 [];
    }
}

// Export
export default new HNScraper();
