/**
 * WebHarvest Pro - Namshi Scraper
 *     // Price
        const priceElement = $('.product-price .price') || $('.c-product-price__current');
        const price = thi
[truncated]
span')?.text().trim() || 
            $('.product-brand')?.text().trim();
        
[truncated]
 [];
    }
}

// Export
export default new NamshiScraper();
