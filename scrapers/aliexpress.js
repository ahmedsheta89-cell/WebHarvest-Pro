/**
 * WebHarvest Pro - AliExpress Scraper
 *     // Main price
        const priceElement = 
[truncated]
elector('[data-pl]');
        const rating = ratingElement?.getAttribute('data-pl') || null;
        
[truncated]
 [];
    }
}

// Export
export default new AliExpressScraper();
