/**
 * WebHarvest Pro - Jumia Scraper
 *     // Wait for content
        await page.waitForSelector('[data-aut-id="productTitle"]', { timeout: 10000 });
[truncated]
.querySelectorAll('[data-aut-id="productPrice"] span')[0]?.text().trim();
        
        const oldPrice = $(
[truncated]
 [];
    }
}

// Export
export default new JumiaScraper();
